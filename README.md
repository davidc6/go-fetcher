# A Simple fetcher in Go

## Instructions

- Start Docker container - `docker run -p 7317:7317 ghcr.io/go-rod/rod`
- Generate the executable `go build`
- Run the executable `go build -o <executable_name>`
